import { ArrayItemDirective } from './array-item.directive';

describe('FormArrayItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrayItemDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
