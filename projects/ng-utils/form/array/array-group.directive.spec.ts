import { ArrayGroupDirective } from './array-group.directive';

describe('ArrayGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrayGroupDirective();
    expect(directive).toBeTruthy();
  });
});
