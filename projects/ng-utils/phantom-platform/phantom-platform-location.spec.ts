import { PhantomPlatformLocation } from './phantom-platform-location';

describe('GhostPlatformLocation', () => {
  it('should create an instance', () => {
    expect(new PhantomPlatformLocation()).toBeTruthy();
  });
});
