import { SubstituteDirective } from './substitute.directive';

describe('SubstituteDirective', () => {
  it('should create an instance', () => {
    const directive = new SubstituteDirective();
    expect(directive).toBeTruthy();
  });
});
