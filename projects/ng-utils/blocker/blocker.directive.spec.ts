import { BlockerDirective } from './blocker.directive';

describe('BlockerDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockerDirective();
    expect(directive).toBeTruthy();
  });
});
